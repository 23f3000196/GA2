name: Q6 Action

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: 23f3000196@ds.study.iitm.ac.in
        run: echo "Hello, world!"
